name: Push

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-18.04
    steps:
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: andreas.hansson
          password: ${{ secrets.GITHUB_PAT }}

          # - name: Login to GitHub Container Registry
      # - uses: docker/login-action@v2
        # with:
          # registry: ghcr.io
          # username: ${{ github.actor }}
          # password: ${{ secrets.GITHUB_PAT }}
        # run: docker login 