name: Build

on:
  push:
    branches:
      - main

jobs:
  Build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Build Docker image
        run: docker build -t main:$(date +%s) .

